##This is a File structure for react native expo app with native wind configuration You just have to clone this repo and do basic setup

1. Install dependencies
   ```sh
   npm install
   ```
2.Install Expo if needed
```sh
npm install -g expo-cli
```
